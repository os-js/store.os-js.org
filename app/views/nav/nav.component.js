// Register the `appNav` component on the `appNav` module,
angular.
  module('appNav', []).
  component('appNav', {
    templateUrl: 'app/parts/nav/nav.template.html',
    controller: function () {
    }
  });